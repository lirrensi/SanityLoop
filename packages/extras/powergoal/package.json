{
  "name": "@sanityloop/powergoal",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --experimental-transform-types --test --test-force-exit \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@sanityloop/core": "^0.1.0",
    "@sanityloop/inputs": "^0.1.0",
    "@sanityloop/util": "^0.1.0"
  },
  "devDependencies": {},
  "files": [
    "**/*.ts",
    "!test",
    "!**/test/**"
  ],
  "engines": {
    "node": ">=22.6"
  },
  "description": "The goal loop — like the loops engine, but a goal persists until someone proves it done."
}
